/**
 * Theme tone constants — maps tone names to CSS custom property names.
 * Used by components that accept a `tone` prop.
 */
export const TONES = {
  sky: 'var(--accent-sky)',
  mint: 'var(--accent-mint)',
  yellow: 'var(--accent-yellow)',
  coral: 'var(--accent-coral)',
  peach: 'var(--accent-peach)',
  lavender: 'var(--accent-lavender)',
}

/**
 * Supported tone class names for CSS `.tone-*` classes.
 */
export const TONE_CLASSES = ['sky', 'mint', 'yellow', 'coral', 'peach', 'lavender']
